// Desafio Classificador de nível de Herói
let nomeDoHeroi= "spartacos333"
let XP = 0
let rankDoHeroi
XP++
switch(XP){
    case XP <= 1000 :
    console.log("O Herói " + nomeDoHeroi + " está no nível " + "a")
    break;
    case XP >= 1001 && XP <= 2000 :
    console.log("O Herói " + nomeDoHeroi + " está no nível " + "b")
    break;
    case XP >= 2001 && XP <= 5000 :
    console.log("O Herói " + nomeDoHeroi + " está no nível " + "c")
    break;
    case XP >= 5001 && XP <= 6000 :
    console.log("O Herói " + nomeDoHeroi + " está no nível " + "d")
    break;
    case XP >= 6001 && XP <= 7000 :
    console.log("O Herói " + nomeDoHeroi + " está no nível " + "f")
    break;
    case XP >= 7001 && XP <= 8000 :
    console.log("O Herói " + nomeDoHeroi + " está no nível " + "g")
    break;
    case XP >= 7001 && XP <= 8000 :
    console.log("O Herói " + nomeDoHeroi + " está no nível " + "h")
    break;
    case XP >= 8001 && XP <= 9000:
        console.log("O Herói " + nomeDoHeroi + " está no nível " + "j");
   
}
